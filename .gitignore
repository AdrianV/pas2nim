nimcache
# build artifacts
pas2nim
pas2nim.exe
legacy/test/testMethod
legacy/pascal/syspas
pas2nimony/nimcache
pas2nimony/bin
pas2nimony/test/tmp
# scratch: logs, old-toolchain export, one-off repro trees (hundreds of MB)
pas2nimony/test/tmp2
# closed-source Delphi compiler food - never published
pas2nimony/test/private
# closed-source Delphi corpus: several nested git checkouts. NEVER publish,
# and never publish anything derived from them (translated .nim, logs, build
# trees) either.
/private/
# the corpus build tree: flattened private sources + generated .nim - derived
# from the private corpus, so it is private too
pas2nimony/test/gonzales/
# scratch
a/
b/
# relocated wine prefixes (Delphi 2007 oracle; never published)
.wine/
with.nim
test.txt
.tmpx/
# harness / editor state
.dsh/
.vscode/
*.log
